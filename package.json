{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "lerna": "^6.3.0"
  },
  "scripts": {
    "dev": "lerna run dev",
    "publish": "lerna run version",
    "test": "lerna run test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express-jwt": "^8.2.1",
    "jsonwebtoken": "^9.0.0"
  }
}
